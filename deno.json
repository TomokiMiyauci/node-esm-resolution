{
  "tasks": {
    "test": "deno test --allow-read --import-map=_test_import_map.json --doc",
    "publish": "deno publish -c=jsr.json"
  },
  "fmt": {
    "exclude": ["CHANGELOG.md"]
  },
  "lint": {
    "exclude": ["CHANGELOG.md"]
  },
  "test": {
    "exclude": ["CHANGELOG.md"]
  }
}
